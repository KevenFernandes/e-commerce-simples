import clsx from "clsx";

export default function UserDashboard() {
  return (
    <section className={clsx("flex items-center justify-center h-screen ")}>
      <div>
        <h1>Não esqueça de atualizar seu perfil</h1>
      </div>
    </section>
  );
}
